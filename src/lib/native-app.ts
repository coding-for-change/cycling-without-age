// Identifiers the site publishes so the OS trusts the app as this relying
// party. Change them together with the Xcode project and the Android manifest.
export const NATIVE_APP = {
  domain: "cwa.codingforchange.com",
  iosTeamId: "8TPMU6R9FN",
  bundleId: "com.codingforchange.cwa",
  androidPackage: "com.codingforchange.cwa",
} as const;
