<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Applied to every notification FCM renders while the app is not in
             the foreground, where no channel or payload icon is given. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_push" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/cwa_notification" />

        <!-- Passkeys: ties the app to the site's relying-party domain via
             https://cwa.codingforchange.com/.well-known/assetlinks.json -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <!-- An empty DSN sends nothing, so builds without SENTRY_DSN_ANDROID in
             the environment stay silent. io.sentry.release is omitted to keep
             the default <applicationId>@<versionName>+<versionCode>. -->
        <meta-data android:name="io.sentry.dsn" android:value="${sentryDsn}" />
        <meta-data android:name="io.sentry.environment" android:value="${sentryEnvironment}" />
        <meta-data android:name="io.sentry.auto-session-tracking.enable" android:value="true" />
        <meta-data android:name="io.sentry.traces.sample-rate" android:value="0" />
        <meta-data android:name="io.sentry.send-default-pii" android:value="false" />
        <meta-data android:name="io.sentry.attach-screenshot" android:value="false" />
        <meta-data android:name="io.sentry.attach-view-hierarchy" android:value="false" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Capacitor's own BridgeWebChromeClient requests these at runtime when
         the WebView asks for a position, so /location needs no Geolocation
         plugin — but an undeclared permission fails the request outright.
         Both are required: the launcher asks for the pair. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
</manifest>
